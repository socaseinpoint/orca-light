- 2026-06-04 — dangling-session = auto-mark compressed (1A) — when resume finds pending sessions but no in-work focus to land a block, mark them compressed and move on; the closing session's substance (the done-move) is already in git + the focus Log, so only marginal why/next/head narrative is lost. Beats appending to a closed focus (violates done-is-terminal, ambiguous with >1 done focus).
- 2026-06-04 — ledger GC = explicit 'orca session prune --days N' (2A) — drop compressed records older than N days (default 30) on demand, not auto-swept at SessionStart-record. Keeps GC visible/under-control and out of the hot hook path; can be wired into a hook later if wanted.
