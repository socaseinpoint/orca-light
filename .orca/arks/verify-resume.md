# ark: verify-resume

thread: build-orca-light
intent: harden verify and design auto-resume so multi-session work carries itself
done-when: resume skill compresses a prior transcript into a verified 4-layer block
state: active
updated: 2026-06-04

## decisions
- 2026-06-04 — verify splits into receipts (deterministic, ours) + a pluggable judge (separate cheap model), because "anchor is real" never proved "anchor supports the claim".
- 2026-06-04 — resume runs on-demand at session start, not on a timer/background; the prior transcript on disk is the durable source, so hard exits cannot lose it.

## sessions
### 2026-06-04
done: stale-handoff detector + verify v2 (zones) + pluggable judge landed. [commit:5b224ed] [test:bash tests/test_stale.sh]
done: verify schema v2 — `done:` is the proven zone (anchor required), why/next/head is free narrative. [test:bash tests/test_zones.sh]
done: pluggable claim-vs-evidence judge (deterministic receipts + a separate Haiku grader); the real Haiku caught a true-anchor/false-claim. [commit:5b224ed] [test:bash tests/test_judge.sh]
why:  the concept oversold verify; the real hole was claim<->evidence, a known field (groundedness), so we delegate judging and keep the receipt layer as ours.
next: build the resume skill — dispatch a subagent to compress the prior session transcript into one 4-layer block, append to the ark, then verify.
head: suite green (verify/zones/gate/stale/views/decide/judge); committed at 5b224ed; proof-handoff archived. next big piece is the resume skill; report cross-project still open.
### 2026-06-04 (session 2)
done: orca archive <slug> — terminal state flag, one writer per ark, idempotent. [commit:b3e4a73] [test:bash tests/test_archive.sh]
done: orca trail <slug> — one ark's sessions rendered oldest->newest, derived on read. [commit:9b52321] [test:bash tests/test_trail.sh]
done: orca-resume skill (spec/resume.md) — subagent compresses prior transcript into a verified 4-layer block; structural test locks the battle-test contracts. [file:skills/orca-resume/SKILL.md:1] [commit:8ece891] [test:bash tests/test_resume.sh]
why:  built lightest-first (archive < trail < skill); the skill carries the LIVE command surface (orca --help) instead of hardcoding, so it can't drift onto dead commands like the battle test caught. (anchor note: the skill file's birth commit is 8ece891, not my 781b35f — a concurrent `git add -A` in the other session swept SKILL.md + test_resume.sh into its commit; the judge caught the wrong anchor. lesson re-learned: one writer per file, and `git add` pathspecs not `-A` when two sessions share a tree.)
next: dogfood orca-resume on a real second session — confirm the subagent dispatch returns a clean block and verify passes against actual git history.
head: all 11 suites green; skill symlinked into ~/.claude/skills (live, shows in skill list). resume flow is validated structurally, not yet end-to-end against a real transcript. report cross-project still the other open thread.
### 2026-06-04 (session 3)
done: fixed block-ordering bug — convention is newest-LAST; latest_session now reads the tail (was returning the oldest block), cmd_trail drops the bogus reversal; spec + orca-resume skill + this ark reconciled to append-at-bottom. [commit:8923189] [test:bash tests/test_trail.sh]
why:  the file always grew downward (append-at-bottom) yet the code assumed newest-first — cheaper and safer to pick newest-last and make every reader consistent than to rewrite every existing ark.
next: FIX2 — orca-resume must derive the transcript dir from the CHOSEN ARK's project root (via registry), not the launch cwd; Claude Code keys transcripts by launch dir, so a resume invoked from a parent dir reads the wrong project's transcripts.
head: FIX1 proven on the battle ark — `orca now` from ~ now surfaces session-3, not session-1. This session is sole owner of bin/orca (other session added location-independent `now` at 8ece891 and will not touch the file).
### 2026-06-04 (session 4)
done: closed the launch-dir seam — orca-resume now derives the transcript dir from the chosen ark's project root via new `orca ark-root <slug>` (registry-resolved), not pwd; skill rewired, caveat documented. [commit:bb34b8e] [test:bash tests/test_ark_root.sh]
why:  orca state is global (symlinked binary + ~/.orca) but Claude Code keys transcripts by launch cwd — different locality. ark-root is pure registry logic with no CC coupling; the ~/.claude path-mangle stays in the skill so the binary never learns about Claude Code internals.
next: dogfood resume end-to-end from a parent dir to confirm ark-root selects the right transcript; `orca report` cross-project polish is the last open thread.
head: 12 suites green; FIX1 + FIX2 both landed and committed with explicit pathspecs (no more `git add -A` while the other session shares the tree). residual: ark-root cannot relocate a PRIOR session that was launched from elsewhere — documented "launch from the project dir" as the habit. held sole ownership of bin/orca the whole session.

### 2026-06-04
done: compressed prior session into the ark's session-4 block (launch-dir seam / `orca ark-root` work) — block authored, judged YES by the live Haiku grader against real git+test evidence, and committed; `orca verify` and `bash tests/test_ark_root.sh` both pass on the result. [commit:1641c4c] [file:.orca/arks/verify-resume.md:37] [test:bash tests/test_ark_root.sh]
why:  this was a resume/handoff turn, not a feature turn — the FIX2 code (registry-resolved `ark-root`, skill rewired to mangle ROOT not pwd) had already landed at bb34b8e; the job here was to prove that block grounds its claims and persist it. The grader was given the strict claim-vs-evidence prompt and returned YES (ark-root command present, SKILL.md rewired +21/-4, caveat in commit msg, 6 test cases green), so the block was safe to commit as docs(ark) with an explicit pathspec.
next: dogfood resume end-to-end from a PARENT dir to confirm `orca ark-root` selects the right project's transcript (the seam is closed in code but never exercised cross-dir live); then `orca report` cross-project polish — the last open thread on this ark.
head: 12 suites green; FIX1 + FIX2 both committed with explicit pathspecs (the `git add -A` cross-session race that mis-anchored SKILL.md at 8ece891 is the standing hazard — two sessions still share this tree). Residual hole the code cannot fix: `ark-root` relocates the CURRENT project root but a PRIOR session launched from elsewhere is still unfindable — mitigated only by the documented "launch from the project dir" habit, not enforced. Watch whether dogfooding surfaces a real cross-dir transcript-resolution bug.

### 2026-06-04
done: two design calls landed. (1) resume skill now warns when pwd≠ark-root, making the closeable half of the launch-dir seam loud instead of silent. (2) archive reworked to location-as-truth — `orca archive` MOVES an ark to `arks/archive/<slug>.md` instead of flipping a `state:` field; live views glob `arks/*.md` flat so archived arks drop out for free, `find_ark_path` keeps trail/ark-root reaching them, `is_open` is location-first with a legacy-field fallback. proof-handoff migrated. test_archive rewritten (15 checks), all 12 suites green. [commit:73263df] [commit:7c4b306] [test:bash tests/test_archive.sh]
why:  the launch-dir binding is a FEATURE (dir=project=zero-config scope); only the silent divergence was a defect, so we warn rather than fight it. For archive, location-as-truth beats a `state:` field because one store can't drift — and the live view already globs flat, so the subdir excludes archived arks with zero filter code. Rejected filename date/index prefixes: mtime + `orca now` recency already serve "jump to freshest", and a prefix would fight slug-as-identity.
next: discuss the resume SKILL.md Step-2 wording with the user in plain Russian — they got tangled in the ark-root / transcript-dir / warn prose and want it simplified. Then the still-open thread: dogfood resume from a PARENT dir live, and `orca report` cross-project polish.
head: location-as-truth is in; legacy `state: archived` arks still read as closed (back-compat, not migrated except proof-handoff). If creation-order (not mtime) ever matters, the agreed move is a `created:` header field, NOT a filename prefix. Two sessions still share this tree — keep committing with explicit pathspecs.

### 2026-06-04
done: closed the open design questions. The Step-2 wording confusion was talked through with the user (no skill edit committed — the prose stays, explanation given verbally). Locked the project-language convention (all artifacts English, chat stays Russian) and translated the one Russian decision back to English. Biggest call: rejected "global was a mistake" — global stays; the real defect was making the cross-project roll-up the DEFAULT on the trust-critical path. Decided per-project resume is the spine, cross-project `now` an opt-in lens. [commit:1263200] [commit:eeb7c82] [test:bash tests/test_archive.sh]
why:  separating "global binary" (harmless, keep) from "cross-project registry + resolve-from-anywhere" (the seam's true source) reframed the whole question — the fix is to demote the roll-up off the hot path, not to delete global. Language convention came from a direct user correction: mixed-language artifacts read inconsistent against an English repo.
next: REBUILD under the per-project-spine decision — make default resume take the pwd repo with no path-mangling (seam gone on the hot path), and turn the cross-project view into an explicit `orca now --all` opt-in. This is the real open work; the earlier "discuss Step-2 wording" and "language fix" next-steps are DONE. Still trailing after that: dogfood resume from a PARENT dir live, `orca report` polish.
head: this block exists because a fresh resume was about to land on a stale `next:` ("discuss Step-2") that was already done — caught live while dogfooding. Lesson: flush the handoff after decisions, not just after code. Per-project rebuild is greenfield-ish but touches `now`/resume/ark-root resolution + the skill's Step 2; expect the skill prose to shrink once mangling leaves the default path.

### 2026-06-04
done: Design/decisions session — no feature code, all docs. Added PRINCIPLES.md (7→8 standing design lenses) [file:PRINCIPLES.md:121] and a new-user GUIDE.md (install→init→full flow) [file:GUIDE.md:214]; logged decisions into the ledger [file:.orca/decisions.md:20], the last being cdfc81a "close-nudge must prove capture" [commit:cdfc81a]. Reworked the resume/continuity model on paper: per-project spine as default, cross-project roll-up demoted to opt-in `now --all`, fork must split ark-with-sessions from bare thread, resume compresses the SET of uncompressed-since-last-block (N=1 is special case), and recorded session-context {ark, transcript-path, live-flag} at SessionStart to close three holes at once (dir-reconstruction, ark-attribution, live-detection).
why:  Dogfooding the rebuild surfaced real defects: a bare-thread "continue" choice made a doomlet thrash with nothing to compress; parallel different-ark sessions let resume grab a LIVE transcript by mtime → silent partial/double compression. North star adopted as the judging lens — serve the user's GOAL conveniently+clearly on the LEAST machinery — which is why loss-edge (#7) and dup-edge (#16) were recognized as one root defect needing both fixes together, and why the recorded-state lean was flipped: "lightest that works" must actually work, and silent corruption isn't working. The close→open nudge loop was justified as self-checking by construction (both derive from the same files), and the close-nudge must PROVE capture to kill the user's close-anxiety.
next: Begin the rebuild implementation against these decisions — start with the MUST tier: record session-context at SessionStart + make resume live-aware (refuse to compress an in-progress transcript) + ark-scoped per-project default.
head: In flight: the cross-session-tree hazard is live — a staged-deletion of .orca/arks/proof-handoff.md (shows D in git status) was left untouched deliberately because another session shares the tree; resolve the dirty tree from a SINGLE session. 'orca roadmap' (future-tense view trio: report/now/roadmap) is parked as a candidate ark, not on the critical path. Watch: nudges must stay high-precision/low-frequency or they train the user to ignore them.

### 2026-06-04
done: Dogfooded the orca-resume skill and rebuilt it to "hide the plumbing." Added a presentation contract + auto-pick/silent-verify/self-clean rewrite to the skill [file:skills/orca-resume/SKILL.md:253] [commit:248f518], logged the driving decision in the ledger [file:.orca/decisions.md:21] [commit:e252ab7], appended a verified session block to the design ark [file:.orca/arks/verify-resume.md:61] [commit:f8c9a68], and cleaned the cross-session tree noise by dropping the migrated proof-handoff ark [commit:ac66c1f].
why:  Live dogfooding exposed the resume *wrapper* as anti-continuity even though its *output* (the compressed verified block) worked: it asked a fork question when only one active ark existed, dumped a half-screen of green anchors as if plumbing were the product, and surfaced orca's OWN archive-migration staged-delete as if it were the user's chore. Judged against the north star — serve the user's GOAL on the LEAST machinery — the fix was to make resume run all the plumbing silently (discovery, ark-root resolution, `orca verify`) and show the user exactly one thing: a one-screen re-orientation (problem→fix→changed→next + the cross-ark fork). The user explicitly asked to feel none of the internals.
next: Close the loop by actually running resume end-to-end across a real session boundary — user closes this session, opens a fresh one, invokes resume — and confirm the new skill delivers the single-screen "last session the problem was X, we did Y, Z changed, next is N, forks are here" narrative with no fork-prompt, no anchor dump, no dirty-tree chore.
head: In flight: the cross-session-tree hazard from the prior design block is now partly resolved (proof-handoff staged-delete committed away), but the deeper MUST-tier rebuild it pointed at — record session-context {ark, transcript-path, live-flag} at SessionStart + make resume refuse to compress a LIVE transcript — is still unbuilt; this session only fixed the presentation layer, not the live-detection/loss-edge defect. Watch: self-clean must touch ONLY orca-created bookkeeping via explicit pathspecs, never the user's own edits — that boundary is the risky part of the new skill.

### 2026-06-04 — SUPERSEDED: continuity moved to the new focus model
done: This ark is now LEGACY. A long design session redesigned orca's whole continuity model and dogfooded the new format by hand — the source of truth is now `.orca/01-finish-orca/focus.md` [file:.orca/01-finish-orca/focus.md:1], with the model captured as 7 decisions [file:.orca/decisions.md:28] [commit:6598e1c]. This ark's own deliverable (resume continuity) is DONE; it survives only as a breadcrumb until the first ark migrates it.
why:  The tooling still runs the OLD global/thread model, so `orca now` keeps reading this file. Rather than break the working binary mid-design, the new model lives as a hand-written focus dir and THIS block points at it — so next session lands clean whether you run `orca now` or open the focus directly.
next: Open `.orca/01-finish-orca/focus.md`, then scope its first ark `arks/01-collapse-binary.md` with /plan — collapse the binary + skill to the project-local git-form model and migrate this legacy ark into the focus.
head: Do NOT keep appending here — this ark is frozen. All new continuity goes in the focus's `## След`. Two-state-via-folders is the locked shape: in-work (`.orca/NN-name/`) | archived (`.orca/archive/NN-name/`).
